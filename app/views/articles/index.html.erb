<h1>These are all my articles</h1>
<p>
	<%= link_to "Create new article", new_article_path %> |  <%= link_to "Home", root_path %>
</p>
<table>
	<tr>
		<th>Title</th>
		<th>Description</th>
	</tr>
	<% @articles.each do |article| %>
		<tr>
			<td><%= article.title %></td>
			<td><%= article.description %></td>
			<td>
				<div class="btn-group" role="group" aria-label="...">
			  	<a href=<%= edit_article_path(article) %> class="btn btn-default btn-success">Update</a>
			  	<a href=<%= article_path(article) %> class="btn btn-primary">Show</a>
			  	<%= link_to "Delete", article_path(article), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger" %>
				</div>
			</td>
		</tr>
	<% end %>
	</table>
